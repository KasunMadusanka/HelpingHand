app.controller('commonFooterController', commonFooterController);
function commonFooterController($scope, $state, $cookies, utilService) {
	
};

app.controller('dashController', dashController);
function dashController($scope, $state, $cookies, utilService) {
	
};

app.controller('footerController', footerController);
function footerController($scope, $state, $cookies, utilService) {
	
};

app.controller('galleryController', galleryController);
function galleryController($scope, $state, $cookies, utilService) {
	
};

app.controller('headerController', headerController);
function headerController($scope, $state, $cookies, utilService) {
	
};

app.controller('homeController', homeController);
function homeController($scope, $state, $cookies, utilService) {
	
};

app.controller('loginController', loginController);
function loginController($scope, $state, $cookies, utilService) {
	
};

app.controller('registerController', registerController);
function registerController($scope, $state, $cookies, utilService) {
	
};

